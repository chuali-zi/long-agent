# kyagent/executor/proxy.py 修改日志

## 2026-05-17  并发审查澄清 C1：supports_parallel_tool_execution 在 POSIX 永为 False

事实
- sandbox.make_preexec_fn 在 Linux 上始终返回非 None 闭包
  （只有 sys.platform == "win32" 才返回 None）。
- 因此 supports_parallel_tool_execution = (self._shared_preexec is None)
  在 Linux 生产环境永远为 False。
- 配合 Agent._is_parallel_safe 中 sys.platform != "win32" 的 gate：
  · Linux 真机：sys.platform != "win32" = True，但 executor.supports = False
                 → 并行整体走不到，所有多工具回合走串行。
  · Windows mock：sys.platform != "win32" = False → 并行整体走不到。
- 结论：**生产环境的并行工具调度路径目前是 dormant 状态。**

这是 bug 还是设计
- 设计。理由：
  · fork() + 多线程父进程 + Python preexec_fn 是 CPython 已知雷区：
    glibc malloc 在子进程死锁、子进程 async-signal-safety 违规等。
  · 在切换 posix_spawn 或确认 preexec 所有操作 fork-safe 之前，
    安全比性能优先。

修改内容
- 仅扩展 supports_parallel_tool_execution 的 docstring，把：
  · 当前在 Linux 上恒为 False 这个事实
  · 安全保守的原因（fork+threads+preexec_fn 风险）
  · 未来切换 posix_spawn 时的解锁路径
  全部写到 docstring 里，避免后续接手者误以为并行在生产真跑。
- 无任何运行时行为变化。

对外影响
- 0。属性返回值不变，调用方代码不变。
- _is_parallel_safe + per-trace 审计锁的代码仍保留，作为"未来扩展点"
  的基础设施，待执行器升级后即可激活。

复核建议
- 在 README / 性能基线文档中也明确写一句"并行路径目前 gate off，
  bench_ask.py 测出的 -34% p50 收益不依赖并行"，避免对外宣称误解。
